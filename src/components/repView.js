import React from 'react';
import {Box} from '@material-ui/core';

const RepView = (props) => {
    return(
        console.log(props)
        <Box>
            
        </Box>
    );
};

export default RepView;