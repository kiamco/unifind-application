import React from 'react';
import {Box} from '@material-ui/core';
import Nav from '../components/nav';

const Home = () => {
    return (
        <Box>
            <Nav />
        </Box>
    );
};

export default Home;
